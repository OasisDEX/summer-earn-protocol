---
description: >-
  Abstract base contract for exchange rate providers with EMA range adjustment.
---

<!-- AUTOGENERATED by forge doc + scripts/docs/clean-forge-docs.mjs — do not edit. Edit the NatSpec in packages/core-contracts/src/utils/exchangeRateProvider/ExchangeRateProviderBase.sol instead. -->

# ExchangeRateProvider
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/utils/exchangeRateProvider/ExchangeRateProviderBase.sol)

**Title:**
ExchangeRateProviderBase

Abstract base contract for exchange rate providers with EMA range adjustment.
This contract serves as a foundation for implementing exchange rate providers
with built-in price smoothing using an Exponential Moving Average (EMA) range.
It defines a common interface for getting exchange rates and their EMAs,
as well as calculating upper and lower bounds based on the EMA range.
Key features:
- Maintains an adjustable EMA range for price smoothing
- Provides abstract functions for current rates and EMAs
- Implements bound calculations based on the EMA range
- Includes a mechanism to set and update the EMA range
The abstraction allows for various implementations while ensuring
consistency across different providers. This design facilitates
easy swapping of providers and promotes code reusability.


## State Variables
### lowerPercentageRange
The EMA (Exponential Moving Average) range for price smoothing


```solidity
Percentage public lowerPercentageRange
```


### upperPercentageRange

```solidity
Percentage public upperPercentageRange
```


### basePrice
The base price for the exchange rate


```solidity
uint256 public basePrice
```


## Functions
### constructor

Constructor to initialize the ExchangeRateProvider


```solidity
constructor(Percentage _lowerPercentageRange, Percentage _upperPercentageRange, uint256 _basePrice) ;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`_lowerPercentageRange`|`Percentage`|Lower EMA range for price smoothing|
|`_upperPercentageRange`|`Percentage`|Upper EMA range for price smoothing|
|`_basePrice`|`uint256`||


### getExchangeRate

Get the current exchange rate


```solidity
function getExchangeRate() public view virtual returns (uint256);
```
**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`uint256`|The current exchange rate|


### getExchangeRateEma

Get the EMA (Exponential Moving Average) of the exchange rate


```solidity
function getExchangeRateEma() public view virtual returns (uint256);
```
**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`uint256`|The EMA of the exchange rate|


### getLowerBound

Get the lower bound of the exchange rate based on the EMA range


```solidity
function getLowerBound() public view virtual returns (uint256);
```
**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`uint256`|The lower bound of the exchange rate|


### getUpperBound

Get the upper bound of the exchange rate based on the EMA range


```solidity
function getUpperBound() public view virtual returns (uint256);
```
**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`uint256`|The upper bound of the exchange rate|


### _setEmaRange

Set a new EMA range

Internal function to update the EMA range, ensuring it's not greater than 100%


```solidity
function _setEmaRange(Percentage _lowerPercentageRange, Percentage _upperPercentageRange) internal virtual;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`_lowerPercentageRange`|`Percentage`|The new lower EMA range to set|
|`_upperPercentageRange`|`Percentage`|The new upper EMA range to set|


### _setBasePrice


```solidity
function _setBasePrice(uint256 _basePrice) internal virtual;
```

### _applyRange

Apply the EMA range to the given price

Abstract function to be implemented by derived contracts


```solidity
function _applyRange(uint256 price) internal view virtual returns (uint256);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`price`|`uint256`|The input price to adjust|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`uint256`|The price after applying the EMA range|


## Events
### EmaRangeUpdated
Emitted when the EMA range is updated


```solidity
event EmaRangeUpdated(Percentage lowerPercentageRange, Percentage upperPercentageRange);
```

**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`lowerPercentageRange`|`Percentage`|The new lower EMA range value|
|`upperPercentageRange`|`Percentage`|The new upper EMA range value|

### BasePriceUpdated
Emitted when the base price is updated


```solidity
event BasePriceUpdated(uint256 basePrice);
```

**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`basePrice`|`uint256`|The new base price value|

## Errors
### EmaRangeTooHigh
Error thrown when the provided EMA range is too high


```solidity
error EmaRangeTooHigh(Percentage emaRange);
```

**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`emaRange`|`Percentage`|The invalid EMA range value|

### InvalidBasePrice
Error thrown when the provided base price is zero


```solidity
error InvalidBasePrice(uint256 basePrice);
```

**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`basePrice`|`uint256`|The invalid base price value|
