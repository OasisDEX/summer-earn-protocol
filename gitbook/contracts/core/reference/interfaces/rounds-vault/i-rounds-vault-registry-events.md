---
description: >-
  Events emitted by the RoundsVaultRegistry.
---

<!-- AUTOGENERATED by forge doc + scripts/docs/clean-forge-docs.mjs — do not edit. Edit the NatSpec in packages/core-contracts/src/interfaces/rounds-vault/IRoundsVaultRegistryEvents.sol instead. -->

# IRoundsVaultRegistryEvents
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/interfaces/rounds-vault/IRoundsVaultRegistryEvents.sol)

**Title:**
IRoundsVaultRegistryEvents

Events emitted by the RoundsVaultRegistry.

These events form the contract that the rounds-vaults subgraph keys off of for discovery.


## Events
### RoundsVaultPairRegistered
Emitted when a new rounds-vault pair is registered against a target FleetCommander.


```solidity
event RoundsVaultPairRegistered(
    bytes32 indexed pairId,
    bytes32 indexed institutionId,
    address indexed targetVault,
    address inputVault,
    address outputVault
);
```

**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`pairId`|`bytes32`|      keccak256 of the target vault address, uniquely identifying the pair|
|`institutionId`|`bytes32`|Institution this pair belongs to (matches InstitutionalVaultRegistry id)|
|`targetVault`|`address`| The FleetCommander both rounds vaults wrap|
|`inputVault`|`address`|  RoundsVaultInput address (zero if only output is registered)|
|`outputVault`|`address`| RoundsVaultOutput address (zero if only input is registered)|

### RoundsVaultPairUpdated
Emitted when one or both vault addresses of an existing pair are replaced.

Used when a flavor that was previously zero is filled in, or when a vault is migrated.


```solidity
event RoundsVaultPairUpdated(bytes32 indexed pairId, address inputVault, address outputVault);
```

### RoundsVaultPairDeactivated
Emitted when a pair is soft-disabled. Data is preserved for indexer auditability.


```solidity
event RoundsVaultPairDeactivated(bytes32 indexed pairId);
```

### RoundsVaultPairReactivated
Emitted when a previously-deactivated pair is re-enabled.


```solidity
event RoundsVaultPairReactivated(bytes32 indexed pairId);
```
