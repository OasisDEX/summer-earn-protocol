---
description: >-
  Interface for Morpho's Universal Rewards Distributor (Merkle-based rewards)
---

<!-- AUTOGENERATED by forge doc + scripts/docs/clean-forge-docs.mjs — do not edit. Edit the NatSpec in packages/core-contracts/src/interfaces/morpho/IUniversalRewardsDistributor.sol instead. -->

# IUniversalRewardsDistributor

## IUniversalRewardsDistributor
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/interfaces/morpho/IUniversalRewardsDistributor.sol)

**Title:**
IUniversalRewardsDistributor

Interface for Morpho's Universal Rewards Distributor (Merkle-based rewards)

This interface is used for factorizing IUniversalRewardsDistributorStaticTyping and
IUniversalRewardsDistributor.

Consider using the IUniversalRewardsDistributor interface instead of this one.


### Functions
#### setRoot

Submits a new Merkle root and optional IPFS hash (subject to timelock)


```solidity
function setRoot(bytes32 newRoot, bytes32 newIpfsHash) external;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`newRoot`|`bytes32`|The new Merkle root|
|`newIpfsHash`|`bytes32`|The optional IPFS hash with metadata about the root|


#### claim

Claims rewards for an account using a Merkle proof


```solidity
function claim(
    address account,
    address reward,
    uint256 claimable,
    bytes32[] memory proof
)
    external
    returns (uint256 amount);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`account`|`address`|The account to claim for|
|`reward`|`address`|The reward token to claim|
|`claimable`|`uint256`|The cumulative claimable amount encoded in the Merkle tree|
|`proof`|`bytes32[]`|The Merkle proof for the claim|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`amount`|`uint256`|The amount of rewards transferred|


### Events
#### Claimed
Emitted when rewards are claimed


```solidity
event Claimed(address indexed account, address indexed reward, uint256 amount);
```

**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`account`|`address`|The account that received the rewards|
|`reward`|`address`|The reward token claimed|
|`amount`|`uint256`|The amount claimed|



## PendingRoot
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/interfaces/morpho/IUniversalRewardsDistributor.sol)

The pending root struct for a merkle tree distribution during the timelock.


```solidity
struct PendingRoot {
/// @dev The submitted pending root.
bytes32 root;
/// @dev The optional ipfs hash containing metadata about the root (e.g. the merkle tree itself).
bytes32 ipfsHash;
/// @dev The timestamp at which the pending root can be accepted.
uint256 validAt;
}
```
