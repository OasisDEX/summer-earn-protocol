---
description: >-
  Struct to store information about an Ark.
---

<!-- AUTOGENERATED by forge doc + scripts/docs/clean-forge-docs.mjs — do not edit. Edit the NatSpec in packages/core-contracts/src/types/FleetCommanderTypes.sol instead. -->

# FleetCommanderTypes

## ArkData
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/types/FleetCommanderTypes.sol)

**Title:**
ArkData

Struct to store information about an Ark.
This struct holds the address of the Ark and the total assets it holds.

used in the caching mechanism for the FleetCommander


```solidity
struct ArkData {
/// @notice The address of the Ark.
address arkAddress;
/// @notice The total assets held by the Ark.
uint256 totalAssets;
}
```



## FleetCommanderDaoParams
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/types/FleetCommanderTypes.sol)

Configuration parameters for the FleetCommanderDao contract


```solidity
struct FleetCommanderDaoParams {
string name;
string details;
string symbol;
address configurationManager;
address accessManager;
address asset;
uint256 initialMinimumBufferBalance;
uint256 initialRebalanceCooldown;
uint256 depositCap;
Percentage initialTipRate;
address tipJar;
}
```

**Properties**

|Name|Type|Description|
|----|----|-----------|
|`name`|`string`|The name of the fleet commander (ERC20)|
|`details`|`string`|A string containing additional information about the fleet commander|
|`symbol`|`string`|The symbol of the fleet commander share token (ERC20)|
|`configurationManager`|`address`|The address of the ConfigurationManager contract|
|`accessManager`|`address`|The address of the ProtocolAccessManager contract|
|`asset`|`address`|The address of the underlying asset for the fleet|
|`initialMinimumBufferBalance`|`uint256`|The initial minimum balance to be kept in the buffer ark|
|`initialRebalanceCooldown`|`uint256`|The initial cooldown period between rebalancing operations|
|`depositCap`|`uint256`|The initial total deposit cap for the fleet|
|`initialTipRate`|`Percentage`|The initial rate for tips/fees|
|`tipJar`|`address`|The address of the tip jar contract|



## FleetCommanderParams
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/types/FleetCommanderTypes.sol)

Configuration parameters for the FleetCommander contract


```solidity
struct FleetCommanderParams {
string name;
string details;
string symbol;
address configurationManager;
address accessManager;
address asset;
uint256 initialMinimumBufferBalance;
uint256 initialRebalanceCooldown;
uint256 depositCap;
Percentage initialTipRate;
}
```

**Properties**

|Name|Type|Description|
|----|----|-----------|
|`name`|`string`|The name of the fleet commander (ERC20)|
|`details`|`string`|A string containing additional information about the fleet commander|
|`symbol`|`string`|The symbol of the fleet commander share token (ERC20)|
|`configurationManager`|`address`|The address of the ConfigurationManager contract|
|`accessManager`|`address`|The address of the ProtocolAccessManager contract|
|`asset`|`address`|The address of the underlying asset for the fleet|
|`initialMinimumBufferBalance`|`uint256`|The initial minimum balance to be kept in the buffer ark|
|`initialRebalanceCooldown`|`uint256`|The initial cooldown period between rebalancing operations|
|`depositCap`|`uint256`|The initial total deposit cap for the fleet|
|`initialTipRate`|`Percentage`|The initial rate for tips/fees|



## FleetCommanderWhitelistParams
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/types/FleetCommanderTypes.sol)

Configuration parameters for the FleetCommanderWhitelist contract


```solidity
struct FleetCommanderWhitelistParams {
string name;
string details;
string symbol;
address configurationManager;
address accessManager;
address asset;
uint256 initialMinimumBufferBalance;
uint256 initialRebalanceCooldown;
uint256 depositCap;
Percentage initialTipRate;
bool isOperatorGatewayOpen;
}
```

**Properties**

|Name|Type|Description|
|----|----|-----------|
|`name`|`string`|The name of the fleet commander (ERC20)|
|`details`|`string`|A string containing additional information about the fleet commander|
|`symbol`|`string`|The symbol of the fleet commander share token (ERC20)|
|`configurationManager`|`address`|The address of the ConfigurationManager contract|
|`accessManager`|`address`|The address of the ProtocolAccessManagerV2 contract|
|`asset`|`address`|The address of the underlying asset for the fleet|
|`initialMinimumBufferBalance`|`uint256`|The initial minimum balance to be kept in the buffer ark|
|`initialRebalanceCooldown`|`uint256`|The initial cooldown period between rebalancing operations|
|`depositCap`|`uint256`|The initial total deposit cap for the fleet|
|`initialTipRate`|`Percentage`|The initial rate for tips/fees|
|`isOperatorGatewayOpen`|`bool`|Initial status of the operator gateway. Entry (deposit/mint) and exit (withdraw/redeem) operations are gated by the operator gateway. When the gateway is closed, only accounts with the OPERATOR_ROLE can perform these actions. When the gateway is open, all whitelisted accounts can perform these actions.|



## FleetConfig
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/types/FleetCommanderTypes.sol)

**Title:**
FleetConfig

Configuration parameters for the FleetCommander contract

This struct encapsulates the mutable configuration settings of a FleetCommander.
These parameters can be updated during the contract's lifecycle to adjust its behavior.


```solidity
struct FleetConfig {
/**
 * @notice The buffer Ark associated with this FleetCommander
 * @dev This Ark is used as a temporary holding area for funds before they are allocated
 *      to other Arks or when they need to be quickly accessed for withdrawals.
 */
IArk bufferArk;
/**
 * @notice The minimum balance that should be maintained in the buffer Ark
 * @dev This value is used to ensure there's always a certain amount of funds readily
 *      available for withdrawals or rebalancing operations. It's denominated in the
 *      smallest unit of the underlying asset (e.g., wei for ETH).
 */
uint256 minimumBufferBalance;
/**
 * @notice The maximum total value of assets that can be deposited into the FleetCommander
 * @dev This cap helps manage the total assets under management and can be used to
 *      implement controlled growth strategies. It's denominated in the smallest unit
 *      of the underlying asset.
 */
uint256 depositCap;
/**
 * @notice The maximum number of rebalance operations in a single rebalance
 */
uint256 maxRebalanceOperations;
/**
 * @notice The address of the staking rewards contract
 */
address stakingRewardsManager;
}
```



## FleetConfigDao
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/types/FleetCommanderTypes.sol)

**Title:**
FleetConfigDao

Configuration parameters for the FleetCommanderDao contract

This struct encapsulates the mutable configuration settings of a FleetCommanderDao.
These parameters can be updated during the contract's lifecycle to adjust its behavior.


```solidity
struct FleetConfigDao {
/**
 * @notice The buffer Ark associated with this FleetCommanderDao
 * @dev This Ark is used as a temporary holding area for funds before they are allocated
 *      to other Arks or when they need to be quickly accessed for withdrawals.
 */
IArk bufferArk;
/**
 * @notice The minimum balance that should be maintained in the buffer Ark
 * @dev This value is used to ensure there's always a certain amount of funds readily
 *      available for withdrawals or rebalancing operations. It's denominated in the
 *      smallest unit of the underlying asset (e.g., wei for ETH).
 */
uint256 minimumBufferBalance;
/**
 * @notice The maximum total value of assets that can be deposited into the FleetCommanderDao
 * @dev This cap helps manage the total assets under management and can be used to
 *      implement controlled growth strategies. It's denominated in the smallest unit
 *      of the underlying asset.
 */
uint256 depositCap;
/**
 * @notice The maximum number of rebalance operations in a single rebalance
 */
uint256 maxRebalanceOperations;
/**
 * @notice Kept for backward compatibility with the old staking rewards manager
 */
address _stakingRewardsManager;
/**
 * @notice The address of the tip jar (DAO vaults do not use the global tip jar)
 */
address tipJar;
}
```



## FleetConfigWhitelist
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/types/FleetCommanderTypes.sol)

**Title:**
FleetConfigWhitelist

Configuration parameters for the FleetCommanderWhitelist contract


```solidity
struct FleetConfigWhitelist {
/**
 * @notice The buffer Ark associated with this FleetCommanderWhitelist
 * @dev This Ark is used as a temporary holding area for funds before they are allocated
 *      to other Arks or when they need to be quickly accessed for withdrawals.
 */
IArk bufferArk;
/**
 * @notice The minimum balance that should be maintained in the buffer Ark
 * @dev This value is used to ensure there's always a certain amount of funds readily
 *      available for withdrawals or rebalancing operations. It's denominated in the
 *      smallest unit of the underlying asset (e.g., wei for ETH).
 */
uint256 minimumBufferBalance;
/**
 * @notice The maximum total value of assets that can be deposited into the FleetCommanderWhitelist
 * @dev This cap helps manage the total assets under management and can be used to
 *      implement controlled growth strategies. It's denominated in the smallest unit
 *      of the underlying asset.
 */
uint256 depositCap;
/**
 * @notice The maximum number of rebalance operations in a single rebalance
 */
uint256 maxRebalanceOperations;
/**
 * @notice The address of the staking rewards contract
 */
address stakingRewardsManager;
/**
 * @notice If true, the operator gateway is open to everyone (subject to whitelist)
 * @dev Entry (deposit/mint) and exit (withdraw/redeem) operations are gated by the operator gateway.
 *      When the gateway is closed, only accounts with the OPERATOR_ROLE can perform these actions.
 *      When the gateway is open, all whitelisted accounts can perform these actions.
 */
bool isOperatorGatewayOpen;
}
```



## RebalanceData
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/types/FleetCommanderTypes.sol)

Data structure for the rebalance event

if the `boardData` or `disembarkData` is not needed, it should be an empty byte array


```solidity
struct RebalanceData {
address fromArk;
address toArk;
uint256 amount;
bytes boardData;
bytes disembarkData;
}
```

**Properties**

|Name|Type|Description|
|----|----|-----------|
|`fromArk`|`address`|The address of the Ark from which assets are moved|
|`toArk`|`address`|The address of the Ark to which assets are moved|
|`amount`|`uint256`|The amount of assets being moved|
|`boardData`|`bytes`|The data to be passed to the `board` function of the `toArk`|
|`disembarkData`|`bytes`|The data to be passed to the `disembark` function of the `fromArk`|
