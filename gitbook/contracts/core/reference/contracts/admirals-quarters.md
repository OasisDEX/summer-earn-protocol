---
description: >-
  This contract uses an OpenZeppelin nonReentrant modifier with transient storage for gas
---

<!-- AUTOGENERATED by forge doc + scripts/docs/clean-forge-docs.mjs — do not edit. Edit the NatSpec in packages/core-contracts/src/contracts/AdmiralsQuarters.sol instead. -->

# AdmiralsQuarters
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/contracts/AdmiralsQuarters.sol)

**Inherits:**
Ownable, [ProtectedMulticall](./protected-multicall.md), ReentrancyGuardTransient, [IAdmiralsQuarters](../interfaces/i-admirals-quarters.md), ConfigurationManaged

**Title:**
AdmiralsQuarters

This contract uses an OpenZeppelin nonReentrant modifier with transient storage for gas
efficiency.

A contract for managing deposits and withdrawals to/from FleetCommander contracts,
with integrated swapping functionality using 1inch Router.

How to use this contract:
1. Deposit tokens: Use `depositTokens` to deposit ERC20 tokens into the contract.
2. Withdraw tokens: Use `withdrawTokens` to withdraw deposited tokens.
3. Enter a fleet: Use `enterFleet` to deposit tokens into a FleetCommander contract.
4. Exit a fleet: Use `exitFleet` to withdraw tokens from a FleetCommander contract.
5. Swap tokens: Use `swap` to exchange one token for another using the 1inch Router.
6. Rescue tokens: Contract owner can use `rescueTokens` to withdraw any tokens stuck in the contract.

Multicall functionality:
This contract inherits from OpenZeppelin's Multicall, allowing multiple function calls to be batched into a single
transaction.
To use Multicall:
1. Encode each function call you want to make as calldata.
2. Pack these encoded function calls into an array of bytes.
3. Call the `multicall` function with this array as the argument.
Example Multicall usage:
bytes[] memory calls = new bytes[](2);
calls[0] = abi.encodeWithSelector(this.depositTokens.selector, tokenAddress, amount);
calls[1] = abi.encodeWithSelector(this.enterFleet.selector, fleetCommanderAddress, tokenAddress, amount);
(bool[] memory successes, bytes[] memory results) = this.multicall(calls);

Security considerations:
- All external functions are protected against reentrancy attacks.
- The contract uses OpenZeppelin's SafeERC20 for safe token transfers.
- Only the contract owner can rescue tokens.
- Ensure that the 1inch Router address provided in the constructor is correct and trusted.
- Since there is no data exchange between calls - make sure all the tokens are returned to the user


## State Variables
### ONE_INCH_ROUTER

```solidity
address public immutable ONE_INCH_ROUTER
```


### NATIVE_PSEUDO_ADDRESS

```solidity
address public immutable NATIVE_PSEUDO_ADDRESS = 0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE
```


### WRAPPED_NATIVE

```solidity
address public immutable WRAPPED_NATIVE
```


### MERKL_DISTRIBUTOR

```solidity
address public immutable MERKL_DISTRIBUTOR = 0x3Ef3D8bA38EBe18DB133cEc108f4D14CE00Dd9Ae
```


### PERMIT2

```solidity
address public constant PERMIT2 = 0x000000000022D473030F116dDEE9F6B43aC78BA3
```


## Functions
### constructor


```solidity
constructor(
    address _oneInchRouter,
    address _configurationManager,
    address _wrappedNative
)
    Ownable(_msgSender())
    ConfigurationManaged(_configurationManager);
```

### depositTokens

Deposits tokens into the contract

Emits a TokensDeposited event


```solidity
function depositTokens(IERC20 asset, uint256 amount) external payable onlyMulticall nonReentrant;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`asset`|`IERC20`|The token to be deposited|
|`amount`|`uint256`|The amount of tokens to deposit|


### withdrawTokens

Withdraws tokens from the contract

Emits a TokensWithdrawn event


```solidity
function withdrawTokens(IERC20 asset, uint256 amount) external payable onlyMulticall nonReentrant;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`asset`|`IERC20`|The token to be withdrawn|
|`amount`|`uint256`|The amount of tokens to withdraw (0 for all)|


### enterFleet

Enters a FleetCommander by depositing tokens

Emits a FleetEntered event


```solidity
function enterFleet(
    address fleetCommander,
    uint256 assets,
    address receiver
)
    external
    payable
    onlyMulticall
    nonReentrant
    returns (uint256 shares);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`fleetCommander`|`address`|The address of the FleetCommander contract|
|`assets`|`uint256`|The amount of inputToken to be deposited (0 for all)|
|`receiver`|`address`|The address to receive the shares|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`shares`|`uint256`|The number of shares received from the FleetCommander|


### enterFleet

Enters a FleetCommander by depositing tokens

Emits a FleetEntered event


```solidity
function enterFleet(
    address fleetCommander,
    uint256 assets,
    address receiver,
    bytes calldata referralCode
)
    external
    payable
    onlyMulticall
    nonReentrant
    returns (uint256 shares);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`fleetCommander`|`address`|The address of the FleetCommander contract|
|`assets`|`uint256`|The amount of inputToken to be deposited (0 for all)|
|`receiver`|`address`|The address to receive the shares|
|`referralCode`|`bytes`|The referral data to be used for the deposit|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`shares`|`uint256`|The number of shares received from the FleetCommander|


### enterFleetWithPermit

Enters a fleet using an ERC-2612 permit signature for tokens


```solidity
function enterFleetWithPermit(
    address owner,
    address fleetCommander,
    uint256 assets,
    bytes calldata referralCode,
    uint256 deadline,
    uint8 v,
    bytes32 r,
    bytes32 s
)
    external
    payable
    onlyMulticall
    nonReentrant
    returns (uint256 shares);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`owner`|`address`|The address providing the tokens and receiving the shares|
|`fleetCommander`|`address`|The address of the fleet commander|
|`assets`|`uint256`|The amount of tokens to deposit|
|`referralCode`|`bytes`|The referral code (empty for no referral)|
|`deadline`|`uint256`|The deadline for the permit signature|
|`v`|`uint8`|The recovery byte of the signature|
|`r`|`bytes32`|Half of the ECDSA signature pair|
|`s`|`bytes32`|Half of the ECDSA signature pair|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`shares`|`uint256`|The amount of shares received|


### enterFleetWithPermit2

Enters a fleet using a Uniswap Permit2 signature transfer


```solidity
function enterFleetWithPermit2(
    address owner,
    address fleetCommander,
    uint256 assets,
    bytes calldata referralCode,
    ISignatureTransfer.PermitTransferFrom calldata permitData,
    bytes calldata signature
)
    external
    payable
    onlyMulticall
    nonReentrant
    returns (uint256 shares);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`owner`|`address`|The address providing the tokens and receiving the shares|
|`fleetCommander`|`address`|The address of the fleet commander|
|`assets`|`uint256`|The amount of tokens to deposit|
|`referralCode`|`bytes`|The referral code (empty for no referral)|
|`permitData`|`ISignatureTransfer.PermitTransferFrom`|The permit transfer from details (token, amount, nonce, deadline)|
|`signature`|`bytes`|The Permit2 signature from the user|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`shares`|`uint256`|The amount of shares received|


### exitFleet

Exits a FleetCommander by withdrawing tokens

Emits a FleetExited event


```solidity
function exitFleet(
    address fleetCommander,
    uint256 assets
)
    external
    payable
    onlyMulticall
    nonReentrant
    returns (uint256 shares);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`fleetCommander`|`address`|The address of the FleetCommander contract|
|`assets`|`uint256`|The amount of shares to withdraw (0 for all)|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`shares`|`uint256`|The amount of assets received from the FleetCommander|


### exitFleetWithPermit2

Exits a fleet using a Uniswap Permit2 signature transfer for shares

Emits a FleetExited event


```solidity
function exitFleetWithPermit2(
    address owner,
    address fleetCommander,
    ISignatureTransfer.PermitTransferFrom calldata permitData,
    bytes calldata signature
)
    external
    payable
    onlyMulticall
    nonReentrant
    returns (uint256 assets);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`owner`|`address`|The address providing the shares and receiving the assets|
|`fleetCommander`|`address`|The address of the fleet commander|
|`permitData`|`ISignatureTransfer.PermitTransferFrom`|The permit transfer from details (token, amount, nonce, deadline)|
|`signature`|`bytes`|The Permit2 signature from the user|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`assets`|`uint256`|The amount of assets received|


### stake

Stakes shares in a FleetCommander

If zero shares are provided, the full balance of the FleetCommander is staked


```solidity
function stake(address fleetCommander, uint256 shares) external payable onlyMulticall nonReentrant;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`fleetCommander`|`address`|The address of the FleetCommander contract|
|`shares`|`uint256`|The amount of shares to stake|


### unstakeAndWithdrawAssets


```solidity
function unstakeAndWithdrawAssets(
    address fleetCommander,
    uint256 shares,
    bool claimRewards
)
    external
    onlyMulticall
    nonReentrant;
```

### swap

Performs a token swap using 1inch Router

Emits a Swapped event


```solidity
function swap(
    IERC20 fromToken,
    IERC20 toToken,
    uint256 assets,
    uint256 minTokensReceived,
    bytes calldata swapCalldata
)
    external
    payable
    onlyMulticall
    nonReentrant
    returns (uint256 swappedAmount);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`fromToken`|`IERC20`|The token to swap from|
|`toToken`|`IERC20`|The token to swap to|
|`assets`|`uint256`||
|`minTokensReceived`|`uint256`|The minimum amount of toToken to receive after the swap|
|`swapCalldata`|`bytes`|The calldata for the 1inch swap|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`swappedAmount`|`uint256`|The amount of toToken received after the swap|


### claimMerkleRewards

Claims merkle rewards for a user


```solidity
function claimMerkleRewards(
    address user,
    uint256[] calldata indices,
    uint256[] calldata amounts,
    bytes32[][] calldata proofs,
    address rewardsRedeemer
)
    external
    onlyMulticall
    nonReentrant;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`user`|`address`|Address to claim rewards for|
|`indices`|`uint256[]`|Array of merkle proof indices|
|`amounts`|`uint256[]`|Array of merkle proof amounts|
|`proofs`|`bytes32[][]`|Array of merkle proof data|
|`rewardsRedeemer`|`address`|Address of the rewards redeemer contract|


### claimGovernanceRewards

Claims governance rewards


```solidity
function claimGovernanceRewards(address govRewardsManager, address rewardToken)
    external
    onlyMulticall
    nonReentrant;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`govRewardsManager`|`address`|Address of the governance rewards manager|
|`rewardToken`|`address`|Address of the reward token to claim|


### claimFleetRewards

Claims rewards from fleet commanders


```solidity
function claimFleetRewards(
    address[] calldata fleetCommanders,
    address rewardToken
)
    external
    onlyMulticall
    nonReentrant;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`fleetCommanders`|`address[]`|Array of FleetCommander addresses|
|`rewardToken`|`address`|Address of the reward token to claim|


### claimFromMerklDistributor

Claims rewards from a merkl distributor


```solidity
function claimFromMerklDistributor(
    address[] calldata users,
    address[] calldata tokens,
    uint256[] calldata amounts,
    bytes32[][] calldata proofs
)
    external
    onlyMulticall
    nonReentrant;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`users`|`address[]`|Array of users to claim rewards for|
|`tokens`|`address[]`|Array of tokens to claim rewards for|
|`amounts`|`uint256[]`|Array of amounts to claim rewards for|
|`proofs`|`bytes32[][]`|Array of merkle proofs|


### _swap

Internal function to perform a token swap using 1inch


```solidity
function _swap(
    IERC20 fromToken,
    IERC20 toToken,
    uint256 assets,
    uint256 minTokensReceived,
    bytes calldata swapCalldata
)
    internal
    returns (uint256 swappedAmount);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`fromToken`|`IERC20`|The token to swap from|
|`toToken`|`IERC20`|The token to swap to|
|`assets`|`uint256`|The amount of fromToken to swap|
|`minTokensReceived`|`uint256`|The minimum amount of toToken to receive after the swap|
|`swapCalldata`|`bytes`|The 1inch swap calldata|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`swappedAmount`|`uint256`|The amount of toToken received from the swap|


### _validateFleetCommander


```solidity
function _validateFleetCommander(address fleetCommander) internal view;
```

### _validateToken


```solidity
function _validateToken(IERC20 token) internal pure;
```

### _validateAmount


```solidity
function _validateAmount(uint256 amount) internal pure;
```

### _validateNativeAmount


```solidity
function _validateNativeAmount(uint256 amount, uint256 msgValue) internal view;
```

### rescueTokens

Allows the owner to rescue any ERC20 tokens sent to the contract by mistake

Can only be called by the contract owner


```solidity
function rescueTokens(IERC20 token, address to, uint256 amount) external onlyOwner;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`token`|`IERC20`|The address of the ERC20 token to rescue|
|`to`|`address`|The address to send the rescued tokens to|
|`amount`|`uint256`|The amount of tokens to rescue|


### receive

Required to receive ETH when unwrapping WETH


```solidity
receive() external payable;
```

### _claimMerkleRewards

Claims rewards from merkle distributor


```solidity
function _claimMerkleRewards(
    address user,
    uint256[] calldata indices,
    uint256[] calldata amounts,
    bytes32[][] calldata proofs,
    address rewardsRedeemer
)
    internal;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`user`|`address`|Address to claim rewards for|
|`indices`|`uint256[]`|Array of merkle proof indices|
|`amounts`|`uint256[]`|Array of merkle proof amounts|
|`proofs`|`bytes32[][]`|Array of merkle proof data|
|`rewardsRedeemer`|`address`|Address of the rewards redeemer contract|


### _claimGovernanceRewards

Claims rewards from governance rewards manager


```solidity
function _claimGovernanceRewards(address govRewardsManager, address rewardToken) internal;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`govRewardsManager`|`address`|Address of the governance rewards manager|
|`rewardToken`|`address`|Address of the reward token to claim|


### _claimFleetRewards

Claims rewards from fleet commanders


```solidity
function _claimFleetRewards(address[] calldata fleetCommanders, address rewardToken) internal;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`fleetCommanders`|`address[]`|Array of FleetCommander addresses|
|`rewardToken`|`address`|Address of the reward token to claim|


### moveFromCompoundToAdmiralsQuarters

Imports a position from a Compound cToken to AdmiralsQuarters, has to be followed by a call to enter
fleet

If zero amount is provided, the full balance of the cToken is imported


```solidity
function moveFromCompoundToAdmiralsQuarters(address cToken, uint256 assets) external onlyMulticall nonReentrant;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`cToken`|`address`|The address of the Compound cToken|
|`assets`|`uint256`||


### moveFromAaveToAdmiralsQuarters

Imports a position from an Aave aToken to AdmiralsQuarters, has to be followed by a call to enter fleet

If zero amount is provided, the full balance of the aToken is imported


```solidity
function moveFromAaveToAdmiralsQuarters(address aToken, uint256 assets) external onlyMulticall nonReentrant;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`aToken`|`address`|The address of the Aave aToken|
|`assets`|`uint256`||


### moveFromERC4626ToAdmiralsQuarters

Imports a position from an ERC4626 vault to AdmiralsQuarters, has to be followed by a call to enter fleet

If zero shares are provided, the full balance of the vault is imported


```solidity
function moveFromERC4626ToAdmiralsQuarters(address vault, uint256 shares) external onlyMulticall nonReentrant;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`vault`|`address`|The address of the ERC4626 vault|
|`shares`|`uint256`|The amount of vault tokens to import|
