---
description: >-
  An Ark implementation for Pendle Principal Tokens with oracle-based pricing.
---

<!-- AUTOGENERATED by forge doc + scripts/docs/clean-forge-docs.mjs — do not edit. Edit the NatSpec in packages/core-contracts/src/contracts/arks/PendlePtOracleArk.sol instead. -->

# PendlePtOracleArk
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/contracts/arks/PendlePtOracleArk.sol)

**Inherits:**
[Ark](../ark.md), [CurveExchangeRateProvider](../../utils/exchangeRateProvider/curve-exchange-rate-provider.md)

**Title:**
PendlePtOracleArk

An Ark implementation for Pendle Principal Tokens with oracle-based pricing.
This contract combines functionality from Ark, CurveExchangeRateProvider,
and Pendle-specific operations to create a yield-generating vault that
works with Pendle's Principal Tokens (PT). It manages deposits, withdrawals,
and automatic rollovers between different Pendle markets.
Terms:
- Ark asset (also Fleet asset eg USDC)
- Market asset (the asset used by a Pendle PT market eg USDe)
- Pendle PT (the Pendle principal token)
Key features:
- Handles boarding (depositing) and disembarking (withdrawing) of tokens
- Manages conversion between Ark assets and Pendle's Principal Tokens
- Implements automatic rollover to new markets upon expiry
- Uses an oracle for accurate PT pricing
- Leverages Curve as a price oracle for stablecoins
- Implements slippage protection for trades
- Includes EMA-based price bounds for trade execution
- Supports reward token harvesting from Pendle markets
- Provides emergency withdrawal functionality for expired markets
Security features:
- Ensures markets aren't too close to expiry when boarding
- Validates receiver addresses and market consistency
- Implements slippage control with configurable tolerances
- Includes oracle duration checks for price reliability
- Enforces market rollover conditions and validation
The contract is designed to provide yield opportunities by leveraging
Pendle's Principal Tokens while ensuring accurate pricing and efficient
fund management. The rollover mechanism allows for continuous yield
generation across multiple market cycles. By combining multiple
functionalities, it offers a sophisticated and flexible system for
managing yield-generating strategies in the DeFi ecosystem.
Integration points:
- Pendle Router: For PT swaps and market interactions
- Curve: For stablecoin exchange rates
- Pendle Oracle: For PT pricing
- ERC20: For token transfers and approvals


## State Variables
### MAX_SLIPPAGE_PERCENTAGE

```solidity
Percentage public constant MAX_SLIPPAGE_PERCENTAGE = PERCENTAGE_100
```


### MIN_ORACLE_DURATION

```solidity
uint256 public constant MIN_ORACLE_DURATION = 15 minutes
```


### MAX_ORACLE_DURATION

```solidity
uint256 public constant MAX_ORACLE_DURATION = 1 hours
```


### marketAsset

```solidity
address public immutable marketAsset
```


### market

```solidity
address public market
```


### router

```solidity
address public immutable router
```


### nextMarket

```solidity
address public nextMarket
```


### oracle

```solidity
address public immutable oracle
```


### configTokenDecimals

```solidity
uint8 public immutable configTokenDecimals
```


### marketAssetDecimals

```solidity
uint8 public immutable marketAssetDecimals
```


### ptDecimals

```solidity
uint8 public ptDecimals
```


### oracleDuration

```solidity
uint32 public oracleDuration
```


### SY

```solidity
IStandardizedYield public SY
```


### PT

```solidity
IPPrincipalToken public PT
```


### YT

```solidity
IPYieldToken public YT
```


### slippagePercentage

```solidity
Percentage public slippagePercentage
```


### marketExpiry

```solidity
uint256 public marketExpiry
```


### routerParams

```solidity
ApproxParams public routerParams
```


### emptyLimitOrderData

```solidity
LimitOrderData emptyLimitOrderData
```


### emptySwap

```solidity
SwapData public emptySwap
```


## Functions
### constructor

Contract constructor


```solidity
constructor(
    ArkParams memory _params,
    PendlePtArkConstructorParams memory _pendlePtArkConstructorParams,
    CurveSwapArkConstructorParams memory _curveSwapArkConstructorParams
)
    Ark(_params)
    CurveExchangeRateProvider(
        _curveSwapArkConstructorParams.curvePool,
        _getSyAssetAddress(_pendlePtArkConstructorParams.market),
        _curveSwapArkConstructorParams.lowerPercentageRange,
        _curveSwapArkConstructorParams.upperPercentageRange,
        _curveSwapArkConstructorParams.basePrice
    );
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`_params`|`ArkParams`|General Ark parameters|
|`_pendlePtArkConstructorParams`|`PendlePtArkConstructorParams`|Pendle PT Ark specific parameters|
|`_curveSwapArkConstructorParams`|`CurveSwapArkConstructorParams`|Curve swap specific parameters|


### _withdrawableTotalAssets

Internal function to get the total assets that are withdrawable

PendlePtOracleArk is not withdrawable by default


```solidity
function _withdrawableTotalAssets() internal pure override returns (uint256);
```

### _getSyAssetAddress

Helper function to get the SY asset address from a market


```solidity
function _getSyAssetAddress(address _market) internal view returns (address assetAddress);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`_market`|`address`|The market address to query|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`assetAddress`|`address`|The asset address from the SY contract|


### totalAssets

Calculates the total assets held by the Ark

We handle this differently based on whether the market has expired:
1. If the market has expired: return the exact PT / LP balance (1:1 ratio)
2. If the market has not expired: subtract slippage from the calculated asset amount


```solidity
function totalAssets() public view override returns (uint256);
```
**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`uint256`|The total assets in underlying token|


### totalAssetsNoSlippage

Calculates the total assets held by the Ark without considering slippage


```solidity
function totalAssetsNoSlippage() public view returns (uint256);
```
**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`uint256`|The total assets in underlying token without slippage|


### validateAndDecodeSwapForPtParams

Validate and decode swap for PT parameters


```solidity
function validateAndDecodeSwapForPtParams(bytes calldata params)
    external
    pure
    returns (
        address receiver,
        address swapMarket,
        uint256 minPtOut,
        ApproxParams memory guessPtOut,
        TokenInput memory input,
        LimitOrderData memory limit
    );
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`params`|`bytes`|Encoded swap parameters|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`receiver`|`address`|Address of the receiver|
|`swapMarket`|`address`|Address of the swap market|
|`minPtOut`|`uint256`|Minimum amount of PT out|
|`guessPtOut`|`ApproxParams`|Guess amount of PT out|
|`input`|`TokenInput`|Token input|
|`limit`|`LimitOrderData`|Limit order data|


### validateAndDecodeSwapPtForTokenParams

Validate and decode swap PT for token parameters


```solidity
function validateAndDecodeSwapPtForTokenParams(bytes calldata params)
    external
    pure
    returns (
        address receiver,
        address swapMarket,
        uint256 exactPtIn,
        TokenOutput memory output,
        LimitOrderData memory limit
    );
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`params`|`bytes`|Encoded swap parameters|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`receiver`|`address`|Address of the receiver|
|`swapMarket`|`address`|Address of the swap market|
|`exactPtIn`|`uint256`|Exact amount of PT in|
|`output`|`TokenOutput`|Token output|
|`limit`|`LimitOrderData`|Limit order data|


### isMarketExpired

Check if the current market has expired


```solidity
function isMarketExpired() public view returns (bool);
```
**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`bool`|bool True if the market has expired, false otherwise|


### withdrawExpiredMarket


```solidity
function withdrawExpiredMarket() public onlyGovernor;
```

### rolloverIfNeeded

Rollover to a new market if needed


```solidity
function rolloverIfNeeded() public;
```

### setNextMarket

Set the next market


```solidity
function setNextMarket(address _nextMarket) public onlyGovernor;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`_nextMarket`|`address`|Address of the next market|


### setSlippagePercentage

Sets the slippage tolerance


```solidity
function setSlippagePercentage(Percentage _slippagePercentage) external onlyCurator(config.commander);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`_slippagePercentage`|`Percentage`|New slippage tolerance|


### setEmaRange


```solidity
function setEmaRange(
    Percentage _lowerPercentageRange,
    Percentage _upperPercentageRange
)
    external
    onlyCurator(config.commander);
```

### setBasePrice


```solidity
function setBasePrice(uint256 _basePrice) external onlyCurator(config.commander);
```

### setOracleDuration

Sets the oracle duration


```solidity
function setOracleDuration(uint32 _oracleDuration) external onlyCurator(config.commander);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`_oracleDuration`|`uint32`|New oracle duration|


### _swapFleetAssetForPt

Internal function to swap fleet asset for Principal Tokens (PT)

This function is called during the boarding process


```solidity
function _swapFleetAssetForPt(uint256 _amount, bytes calldata data) internal shouldBuy;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`_amount`|`uint256`|Amount of fleet asset to swap for PT|
|`data`|`bytes`|Additional data for the swap|


### _swapPtForFleetAsset

Internal function to swap Principal Tokens (PT) for fleet asset

This function is called during the disembarking process


```solidity
function _swapPtForFleetAsset(uint256 _amount, bytes calldata data) internal;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`_amount`|`uint256`|Minimum amount of fleet asset to receive from swap|
|`data`|`bytes`|Additional data for the swap|


### _board

Internal function to board (deposit) tokens


```solidity
function _board(uint256 amount, bytes calldata data) internal override;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`amount`|`uint256`|Amount of tokens to board|
|`data`|`bytes`|Additional data for boarding|


### _disembark

Internal function to disembark (withdraw) tokens


```solidity
function _disembark(uint256 amount, bytes calldata data) internal override shouldTrade;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`amount`|`uint256`|Amount of tokens to disembark|
|`data`|`bytes`|Additional data for disembarking|


### _harvest

Internal function to harvest rewards


```solidity
function _harvest(bytes calldata)
    internal
    override
    returns (address[] memory rewardTokens, uint256[] memory rewardAmounts);
```
**Returns**

|Name|Type|Description|
|----|----|-----------|
|`rewardTokens`|`address[]`|The addresses of the reward tokens|
|`rewardAmounts`|`uint256[]`|The amounts of the reward tokens|


### _validateBoardData

Validate board data


```solidity
function _validateBoardData(bytes calldata data) internal view override;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`data`|`bytes`|Data to validate|


### _validateDisembarkData

Validate disembark data


```solidity
function _validateDisembarkData(bytes calldata data) internal view override;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`data`|`bytes`|Data to validate|


### _setupRouterParams

Set up router parameters


```solidity
function _setupRouterParams() internal;
```

### _updateMarketData

Update market data


```solidity
function _updateMarketData() internal;
```

### _updateMarketAndTokens

Update market and token addresses


```solidity
function _updateMarketAndTokens(address newMarket) internal;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`newMarket`|`address`|Address of the new market|


### _ptToMarketAssetRate

Get the PT to asset rate


```solidity
function _ptToMarketAssetRate() internal view returns (uint256);
```
**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`uint256`|The PT to asset rate|


### _marketAssetToPt

Convert market asset amount to Principal Tokens (PT)


```solidity
function _marketAssetToPt(uint256 amount) internal view returns (uint256);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`amount`|`uint256`|Amount of market assets to convert|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`uint256`|The equivalent amount of Principal Tokens (PT)|


### _fleetAssetToPt

Converts fleet asset amount to PT amount


```solidity
function _fleetAssetToPt(uint256 fleetAssetAmount) internal view returns (uint256);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`fleetAssetAmount`|`uint256`|Amount of fleet assets to convert|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`uint256`|ptAmount The equivalent amount of Principal Tokens|


### _depositMarketAssetForPt

Deposits tokens and swaps them for Principal Tokens (PT)


```solidity
function _depositMarketAssetForPt(uint256 _amount) internal;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`_amount`|`uint256`|Amount of tokens to deposit|


### _rolloverIfNeeded

Rollover to a new market if needed


```solidity
function _rolloverIfNeeded() internal;
```

### _isOracleReady

Checks if the Pendle oracle is ready for the given market


```solidity
function _isOracleReady(address _market) internal view returns (bool);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`_market`|`address`|The address of the Pendle market to check|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`bool`|bool Returns true if the oracle is ready, false otherwise|


### _redeemMarketAssetFromPtPostExpiry

Redeem tokens from PT post expiry


```solidity
function _redeemMarketAssetFromPtPostExpiry(uint256 ptAmount, uint256 minTokenOut) internal;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`ptAmount`|`uint256`|Amount of PT to redeem|
|`minTokenOut`|`uint256`|Minimum amount of tokens to receive|


### _shouldTrade

Check if trading should be allowed


```solidity
function _shouldTrade() internal view;
```

### onlyWhenBetween

Check if a number is between two values


```solidity
function onlyWhenBetween(uint256 number, uint256 lower, uint256 upper) internal pure returns (bool);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`number`|`uint256`|The number to check|
|`lower`|`uint256`|The lower bound|
|`upper`|`uint256`|The upper bound|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`bool`|bool Whether the number is between the bounds|


### shouldBuy

Modifier to check if buying should be allowed

This modifier ensures that the market is not expired and that the market expiration is not too close to the
current block timestamp.


```solidity
modifier shouldBuy() ;
```

### shouldTrade

Modifier to check if trading should be allowed


```solidity
modifier shouldTrade() ;
```

## Events
### MarketRolledOver

```solidity
event MarketRolledOver(address newMarket);
```

### SlippageUpdated

```solidity
event SlippageUpdated(Percentage slippagePercentage);
```

### OracleDurationUpdated

```solidity
event OracleDurationUpdated(uint32 oracleDuration);
```

## Errors
### MarketExpired

```solidity
error MarketExpired();
```

### InvalidParamsLength

```solidity
error InvalidParamsLength();
```

### InvalidFunctionSelector

```solidity
error InvalidFunctionSelector();
```

### InvalidReceiver

```solidity
error InvalidReceiver();
```

### InvalidMarket

```solidity
error InvalidMarket();
```

### EmaOutOfRange

```solidity
error EmaOutOfRange();
```

### MarketExpirationTooClose

```solidity
error MarketExpirationTooClose();
```

### InsufficientOutputAmount

```solidity
error InsufficientOutputAmount();
```

### InvalidNextMarket

```solidity
error InvalidNextMarket();
```

### SlippagePercentageTooHigh

```solidity
error SlippagePercentageTooHigh(Percentage slippagePercentage, Percentage maxSlippagePercentage);
```

### OracleDurationTooLow

```solidity
error OracleDurationTooLow(uint32 oracleDuration, uint256 minOracleDuration);
```

### OracleDurationTooHigh

```solidity
error OracleDurationTooHigh(uint32 oracleDuration, uint256 maxOracleDuration);
```

### InvalidAssetForSY

```solidity
error InvalidAssetForSY();
```

### InvalidRouterAddress

```solidity
error InvalidRouterAddress(address router);
```

### InvalidOracleAddress

```solidity
error InvalidOracleAddress(address oracle);
```

### InvalidMarketAddress

```solidity
error InvalidMarketAddress(address market);
```

### InvalidAsset

```solidity
error InvalidAsset(address asset);
```

### InvalidAmount

```solidity
error InvalidAmount();
```

## Structs
### CurveSwapArkConstructorParams

```solidity
struct CurveSwapArkConstructorParams {
    address curvePool;
    uint256 basePrice;
    Percentage lowerPercentageRange;
    Percentage upperPercentageRange;
}
```

### PendlePtArkConstructorParams

```solidity
struct PendlePtArkConstructorParams {
    address market;
    address oracle;
    address router;
}
```

### BoardData

```solidity
struct BoardData {
    bytes swapForPtParams;
}
```

### DisembarkData

```solidity
struct DisembarkData {
    bytes swapPtForTokenParams;
}
```
