---
description: >-
  This contract provides configuration management for the FleetCommander
---

<!-- AUTOGENERATED by forge doc + scripts/docs/clean-forge-docs.mjs — do not edit. Edit the NatSpec in packages/core-contracts/src/contracts/FleetCommanderConfigProvider.sol instead. -->

# FleetCommanderConfigProvider
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/contracts/FleetCommanderConfigProvider.sol)

**Inherits:**
ProtocolAccessManaged, [FleetCommanderPausable](./fleet-commander-pausable.md), ConfigurationManaged, [IFleetCommanderConfigProvider](../interfaces/i-fleet-commander-config-provider.md)

**Title:**
FleetCommanderConfigProvider

**Author:**
SummerFi

This contract provides configuration management for the FleetCommander

**Note:**
see: IFleetCommanderConfigProvider


## State Variables
### config

```solidity
FleetConfig public config
```


### details

```solidity
string public details
```


### _activeArks

```solidity
EnumerableSet.AddressSet private _activeArks
```


### MAX_REBALANCE_OPERATIONS

```solidity
uint256 public constant MAX_REBALANCE_OPERATIONS = 50
```


### INITIAL_MINIMUM_PAUSE_TIME

```solidity
uint256 public constant INITIAL_MINIMUM_PAUSE_TIME = 2 days
```


### transfersEnabled

```solidity
bool public transfersEnabled
```


## Functions
### constructor


```solidity
constructor(FleetCommanderParams memory params)
    ProtocolAccessManaged(params.accessManager)
    FleetCommanderPausable(INITIAL_MINIMUM_PAUSE_TIME)
    ConfigurationManaged(params.configurationManager);
```

### onlyActiveArk

Modifier to restrict function access to only active Arks (excluding the buffer ark)

**Notes:**
- internal-logic: 
- Checks if the provided arkAddress is in the _activeArks set
- If not found, reverts with FleetCommanderArkNotFound error
- If the arkAddress is the buffer ark, it will revert, due to the buffer ark being a special case

- effects: 
- No direct state changes, but may revert the transaction

- security-considerations: 
- Ensures that only active Arks can perform certain operations
- Prevents unauthorized access from inactive or non-existent Arks
- Critical for maintaining the integrity and security of Ark-specific operations


```solidity
modifier onlyActiveArk(address arkAddress) ;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`arkAddress`|`address`|The address of the Ark to check|


### isArkActiveOrBufferArk

Checks if the ark is part of the fleet or is the buffer ark


```solidity
function isArkActiveOrBufferArk(address arkAddress) public view returns (bool);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`arkAddress`|`address`||

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`bool`|bool Returns true if the ark is active or the buffer ark, false otherwise.|


### arks

Retrieves the ark address at the specified index


```solidity
function arks(uint256 index) public view returns (address);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`index`|`uint256`|The index of the ark in the arks array|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`<none>`|`address`|The address of the ark at the specified index|


### getActiveArks

Retrieves the arks currently linked to fleet (excluding the buffer ark)


```solidity
function getActiveArks() public view returns (address[] memory);
```

### getConfig

Retrieves the current fleet config


```solidity
function getConfig() external view override returns (FleetConfig memory);
```

### bufferArk

Retrieves the buffer ark address


```solidity
function bufferArk() external view returns (address);
```

### addArk

Adds a new Ark


```solidity
function addArk(address ark) external onlyGovernor whenNotPaused;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`ark`|`address`|The address of the new Ark|


### removeArk

Removes an existing Ark


```solidity
function removeArk(address ark) external onlyGovernor whenNotPaused;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`ark`|`address`|The address of the Ark to remove|


### setArkDepositCap

Sets a new deposit cap for an Ark


```solidity
function setArkDepositCap(
    address ark,
    uint256 newDepositCap
)
    external
    onlyCurator(address(this))
    onlyActiveArk(ark)
    whenNotPaused;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`ark`|`address`|The address of the Ark|
|`newDepositCap`|`uint256`|The new deposit cap|


### setArkMaxDepositPercentageOfTVL

Sets the max deposit percentage of TVL for an Ark


```solidity
function setArkMaxDepositPercentageOfTVL(
    address ark,
    Percentage newMaxDepositPercentageOfTVL
)
    external
    onlyCurator(address(this))
    onlyActiveArk(ark)
    whenNotPaused;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`ark`|`address`|The address of the Ark|
|`newMaxDepositPercentageOfTVL`|`Percentage`|The new max deposit percentage of TVL|


### setArkMaxRebalanceOutflow

Sets the maxRebalanceOutflow for an Ark

Only callable by the governor


```solidity
function setArkMaxRebalanceOutflow(
    address ark,
    uint256 newMaxRebalanceOutflow
)
    external
    onlyCurator(address(this))
    onlyActiveArk(ark)
    whenNotPaused;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`ark`|`address`|The address of the Ark|
|`newMaxRebalanceOutflow`|`uint256`|The new maxRebalanceOutflow value|


### setArkMaxRebalanceInflow

Sets the maxRebalanceInflow for an Ark

Only callable by the governor


```solidity
function setArkMaxRebalanceInflow(
    address ark,
    uint256 newMaxRebalanceInflow
)
    external
    onlyCurator(address(this))
    onlyActiveArk(ark)
    whenNotPaused;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`ark`|`address`|The address of the Ark|
|`newMaxRebalanceInflow`|`uint256`|The new maxRebalanceInflow value|


### setMinimumBufferBalance

Sets the minimum buffer balance for the fleet commander.


```solidity
function setMinimumBufferBalance(uint256 newMinimumBalance) external onlyCurator(address(this)) whenNotPaused;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`newMinimumBalance`|`uint256`|The new minimum buffer balance to be set.|


### setFleetDepositCap

Sets a new deposit cap for Fleet


```solidity
function setFleetDepositCap(uint256 newCap) external onlyCurator(address(this)) whenNotPaused;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`newCap`|`uint256`||


### updateStakingRewardsManager

Deploys and sets the staking rewards manager contract address


```solidity
function updateStakingRewardsManager() external onlyCurator(address(this)) whenNotPaused;
```

### setMaxRebalanceOperations

Sets the minimum number of allowe rebalance operations.


```solidity
function setMaxRebalanceOperations(uint256 newMaxRebalanceOperations)
    external
    onlyCurator(address(this))
    whenNotPaused;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`newMaxRebalanceOperations`|`uint256`|The new maximum allowed rebalance operations.|


### setFleetTokenTransferability

Enables or disables transfers of fleet commander shares

Only callable by the governor when not paused


```solidity
function setFleetTokenTransferability() external onlyGovernor whenNotPaused;
```

### _addArk

Internal function to add a new Ark to the fleet

**Notes:**
- internal-logic: 
- Checks if the ark address is valid (not zero)
- Verifies the ark is not already active
- Sets the ark as active and determines its withdrawability
- Checks if the ark already has a commander
- Registers this contract as the ark's FleetCommander
- Adds the ark to the list of active arks

- effects: 
- Modifies isArkActiveOrBufferArk mapping
- Updates the arks array
- Emits an ArkAdded event

- security-considerations: 
- Ensures no duplicate arks are added
- Prevents adding arks that already have a commander
- Only callable internally, typically by privileged roles


```solidity
function _addArk(address ark) internal;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`ark`|`address`|The address of the Ark to be added|


### _removeArk

Internal function to remove an Ark from the fleet

**Notes:**
- internal-logic: 
- Checks if the ark is currently active
- Locates and removes the ark from the active arks list
- Validates that the ark can be safely removed
- Marks the ark as inactive
- Unregisters this contract as the ark's FleetCommander
- Revokes the COMMANDER_ROLE for this contract on the ark

- effects: 
- Modifies the isArkActiveOrBufferArk mapping
- Updates the arks array
- Changes the ark's FleetCommander status
- Revokes a role in the access manager
- Emits an ArkRemoved event

- security-considerations: 
- Ensures only active arks can be removed
- Validates ark state before removal to prevent inconsistencies
- Only callable internally, typically by privileged roles


```solidity
function _removeArk(address ark) internal onlyActiveArk(ark);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`ark`|`address`|The address of the Ark to be removed|


### _validateArkRemoval

Internal function to validate if an Ark can be safely removed

**Notes:**
- internal-logic: 
- Checks if the ark's deposit cap is zero
- Verifies that the ark holds no assets

- effects: 
- No direct state changes, but may revert the transaction

- security-considerations: 
- Prevents removal of arks with non-zero deposit caps or assets
- Ensures arks are in a safe state before removal
- Critical for maintaining the integrity of the fleet


```solidity
function _validateArkRemoval(address ark) internal view;
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`ark`|`address`|The address of the Ark to be validated for removal|
