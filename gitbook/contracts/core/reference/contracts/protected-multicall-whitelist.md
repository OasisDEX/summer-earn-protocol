---
description: >-
  Fork of OpenZeppelin's `Multicall` that lets inheriting contracts gate functions on the
---

<!-- AUTOGENERATED by forge doc + scripts/docs/clean-forge-docs.mjs — do not edit. Edit the NatSpec in packages/core-contracts/src/contracts/ProtectedMulticallWhitelist.sol instead. -->

# ProtectedMulticallWhitelist
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/contracts/ProtectedMulticallWhitelist.sol)

**Inherits:**
Context, [Whitelist](../utils/Whitelist/whitelist.md)

**Title:**
ProtectedMulticallWhitelist

Fork of OpenZeppelin's `Multicall` that lets inheriting contracts gate functions on the
"are we inside an active multicall by this caller?" predicate via `onlyMulticall`. The
original caller is stashed in transient storage on `multicall` entry and cleared on exit.

Considerations carried over from OZ `Multicall`:
- Any assumption about calldata validation performed by the sender may be violated if the
sender is not careful about sending transactions invoking [multicall](/src/contracts/ProtectedMulticallWhitelist.sol/abstract.ProtectedMulticallWhitelist.md#multicall); e.g. a relay that
filters function selectors won't filter calls nested within `multicall`.
- Since OZ 5.0.1 and 4.9.4, the implementation forwards the trailing `_contextSuffixLength`
bytes of `msg.data` to subcalls when `msg.sender != _msgSender()`.

WARNING: this contract is NOT safe to use behind an ERC-2771 trusted forwarder. On `multicall`
entry, `_setCaller(msg.sender)` stashes the forwarder address into transient storage, while
`onlyMulticall` compares `_getCaller()` against `_msgSender()` (which unwraps to the end
user). The two values will never match when invoked via a forwarder, so any `onlyMulticall`
gated subcall will revert with `NotMulticall()`. Use direct calls only.

Whitelist behavior:
- This contract inherits from `Whitelist` but does NOT gate `multicall` itself; the entry
point is fully public.
- Inheriting contracts are responsible for gating individual functions, both with their own
whitelist context check and with `onlyMulticall`. `AdmiralsQuartersWhitelist` does both.


## State Variables
### CALLER_KEY
Transient-storage slot key holding the original caller while a `multicall` is in flight.


```solidity
bytes32 constant CALLER_KEY = keccak256("admirals-quarters-caller")
```


## Functions
### onlyMulticall

Ensures the wrapped function is reachable only from within an active `multicall`
initiated by the same `_msgSender()`. Reverts with `NotMulticall` otherwise.


```solidity
modifier onlyMulticall() ;
```

### multicall

Executes a batch of function calls on this contract in a single transaction. Each
entry in `data` is `delegatecall`ed against `address(this)`. The original caller is
recorded in transient storage so `onlyMulticall`-gated functions can verify they are
being invoked nested inside this call.

Reverts with `MulticallAlreadyInProgress` if a `multicall` is already in flight on this
contract (the transient-storage caller slot is non-zero) — direct nesting is rejected to
keep the gate unambiguous. Forwards trailing `_contextSuffixLength` calldata bytes for
ERC-2771 compatibility.

**Note:**
oz-upgrades-unsafe-allow-reachable: delegatecall


```solidity
function multicall(bytes[] calldata data) external payable returns (bytes[] memory results);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`data`|`bytes[]`|Array of ABI-encoded calldata payloads to execute in order|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`results`|`bytes[]`|Array of raw return data per call, aligned with `data`|


### _multicall

Inner loop of `multicall`. `delegatecall`s each payload against `address(this)`,
appending the ERC-2771 context suffix when the call is going through a forwarder.


```solidity
function _multicall(bytes[] calldata data) internal returns (bytes[] memory results);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`data`|`bytes[]`|Array of ABI-encoded calldata payloads to execute in order|

**Returns**

|Name|Type|Description|
|----|----|-----------|
|`results`|`bytes[]`|Array of raw return data per call, aligned with `data`|


### _setCaller

Stashes the active multicall caller in transient storage.


```solidity
function _setCaller(address caller) internal;
```

### _getCaller

Reads the active multicall caller from transient storage. Returns `address(0)` when no
multicall is in flight.


```solidity
function _getCaller() internal view returns (address);
```

## Errors
### MulticallAlreadyInProgress
Reverts when `multicall` is invoked while another `multicall` is already in progress
on this contract (i.e. the transient-storage caller slot is non-zero).


```solidity
error MulticallAlreadyInProgress();
```

### NotMulticall
Reverts when a function gated by `onlyMulticall` is invoked outside the scope of an
active `multicall` call by the same `_msgSender()`.


```solidity
error NotMulticall();
```
