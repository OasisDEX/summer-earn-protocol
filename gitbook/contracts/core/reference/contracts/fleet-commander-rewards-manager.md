---
description: >-
  Contract for managing staking rewards specific to the Fleet system
---

<!-- AUTOGENERATED by forge doc + scripts/docs/clean-forge-docs.mjs — do not edit. Edit the NatSpec in packages/core-contracts/src/contracts/FleetCommanderRewardsManager.sol instead. -->

# FleetCommanderRewardsManager
[Git Source](https://github.com/OasisDEX/summer-earn-protocol/blob/main/packages/core-contracts/src/contracts/FleetCommanderRewardsManager.sol)

**Inherits:**
[IFleetCommanderRewardsManager](../interfaces/i-fleet-commander-rewards-manager.md), StakingRewardsManagerBase

**Title:**
FleetCommanderRewardsManager

Contract for managing staking rewards specific to the Fleet system

Extends StakingRewardsManagerBase with Fleet-specific functionality


## State Variables
### fleetCommander

```solidity
address public immutable fleetCommander
```


## Functions
### constructor

Initializes the FleetStakingRewardsManager contract


```solidity
constructor(address _accessManager, address _fleetCommander) StakingRewardsManagerBase(_accessManager);
```
**Parameters**

|Name|Type|Description|
|----|----|-----------|
|`_accessManager`|`address`|Address of the AccessManager contract|
|`_fleetCommander`|`address`|Address of the FleetCommander contract|


### stakeOnBehalfOf


```solidity
function stakeOnBehalfOf(address receiver, uint256 amount) external override updateReward(receiver);
```

### notifyRewardAmount


```solidity
function notifyRewardAmount(
    address rewardToken,
    uint256 reward,
    uint256 newRewardsDuration
)
    external
    override(StakingRewardsManagerBase, IStakingRewardsManagerBase)
    onlyGovernor
    updateReward(address(0));
```

### unstakeAndWithdrawOnBehalfOf


```solidity
function unstakeAndWithdrawOnBehalfOf(
    address owner,
    uint256 amount,
    bool claimRewards
)
    external
    override
    updateReward(owner);
```
