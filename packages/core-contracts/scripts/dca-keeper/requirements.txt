aiohttp>=3.9,<4
web3>=6.15,<8
python-dotenv>=1.0,<2
# Lambda one-shot mode fetches secrets from SSM. boto3 ships in the AWS Lambda
# base image; pinned here for local Docker / venv parity.
boto3>=1.34,<2
